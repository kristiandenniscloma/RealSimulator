# Frontend controller

This is a static browser app with no build step. Run `python3 -m http.server
8080` in this folder and open <http://localhost:8080>. Connection settings are
saved in browser local storage. On HTTPS, use a `wss://.../ws` backend URL.
